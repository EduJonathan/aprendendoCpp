#include <iostream>
#include <algorithm>
#include <random>

/**
 * A header <random> foi introduzida no C++11 para fornecer um sistema completo de geração de números aleatórios,
 * muito mais robusto e flexível do que o antigo rand() do C. Detalhes a header <algorithm> só tem funções que
 * podem usar aleatoriedade (como shuffle) quando você passa um motor, sendo assim, não fornecem geração direta
 * de números aleatórios.
 */

int main(int argc, char **argv)
{
    /* code */
    return 0;
}
